using System.Collections.Generic;

namespace Editor.Excels
{
    public interface IExcelWriter
    {
        // List<object> GetRowsByID(int id);
        // void ChangeCellValue(int id, params (string, object)[] values);
        // void InsertNewRow(int id, List<object> values);
        // void ReplaceRow(string sheetName, int id, params (string, object)[] v);
        // void DeleteRow(int id);
    }
}